<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_alarm_screen"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@color/alarm_screen_bg"
    tools:context="lifelessplanetinc.kpo_kurs_gpsalarm.AlarmScreen">

    <TextView
        android:id="@+id/AlarmScreenNameET"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="36dp"
        android:textColor="@color/alarm_screen_dark_text"
        android:text="Alarm"
        android:layout_gravity="top|center"/>
    <TextView
        android:id="@+id/AlarmScreenDestET"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="16dp"
        android:text="destination"
        android:textColor="@color/alarm_screen_orange_text"
        android:layout_gravity="top|center"
        android:layout_marginTop="56dp"
        android:marqueeRepeatLimit="marquee_forever"/>

    <ImageButton
        android:layout_width="250dp"
        android:layout_height="250dp"
        android:layout_gravity="center|bottom"
        android:id="@+id/AlarmOFFImb"
        android:background="@android:color/transparent"
        android:src="@drawable/stopbuttonbg"/>
</android.support.design.widget.CoordinatorLayout>
